#include "main.h"

/**
 * mul(int a, int b)- function that multiplies the two numbers
 * @a:first digit
 * @b:second digit
 * Return: returns the value got 
 */

int mul(int a, int b)
{
	return(a*b);
}
